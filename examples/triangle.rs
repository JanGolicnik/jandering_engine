// use jandering_engine::engine::Engine;
fn main() {
    // env_logger::init();

    // let engine = Engine::default();

    // let mut triangle = vec![jandering_engine::object::primitives::triangle(
    //     &engine.renderer,
    // )];

    // engine.run(move |renderer, encoder, plugins, surface, shaders, _| {
    //     renderer.render(&mut triangle, encoder, plugins, surface, shaders);
    // });
}
